# NOTE: backend/Dockerfile is added in Plan 5 (deploy). Not yet runnable.
# To run locally: cd backend && python -m app.main
services:
  backend:
    build: ./backend
    working_dir: /app
    command: python -m app.main
    environment:
      - VOUCH_CORS_ORIGINS=http://localhost:3000
    ports:
      - "8000:8000"
